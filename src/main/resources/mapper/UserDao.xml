<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.abc.springdemo.dao.UserDao">
    <resultMap id="UserResult" type="com.abc.springdemo.pojo.UserDB">
        <result property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="password" column="password"/>
    </resultMap>

    <select id="findAllUsers" resultType="UserResult">
        select id,name,password from t_user order by id desc
    </select>
    
    <insert id="insertUser" parameterType="userDB">
        insert into t_user(name, password) values (#{name}, #{password})
    </insert>

    <update id="updUser" parameterType="userDB">
        update t_user set name=#{name},password=#{password} where id = #{id}
    </update>

    <delete id="delUser" parameterType="long">
        delete from t_user where id=#{id}
    </delete>
</mapper>